<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Đăng nhập - Bán đồ dùng học tập</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #e0f7fa 0%, #fff3e0 100%);
    }
    .form-container {
      background: white;
      border-radius: 1rem;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      padding: 2rem;
      max-width: 500px;
      margin: 2rem auto;
    }
    .form-container h2 {
      color: #1e40af;
      font-weight: 700;
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .form-control:focus {
      border-color: #f97316;
      box-shadow: 0 0 0 0.2rem rgba(249, 115, 22, 0.25);
    }
    .btn-primary {
      background-color: #f97316;
      border: none;
      transition: background-color 0.3s ease;
    }
    .btn-primary:hover {
      background-color: #ea580c;
    }
    .error-message {
      color: #dc2626;
      font-size: 0.875rem;
      display: none;
      margin-top: 0.25rem;
    }
    .input-group-text {
      background-color: #e0f7fa;
      color: #1e40af;
    }
  </style>
</head>
<body>
<%- include('../partials/header') %>

  <div class="form-container">
    <h2>Đăng nhập</h2>
    <form id="loginForm" action="/api/login" method="POST">
      <div class="mb-3">
        <label for="email" class="form-label text-gray-700">Email</label>
        <div class="input-group">
          <span class="input-group-text"><i class="fas fa-envelope"></i></span>
          <input type="email" class="form-control" id="email" name="email" placeholder="Nhập email" required>
        </div>
        <div id="email_error" class="error-message">Email không hợp lệ</div>
      </div>
      <div class="mb-3">
        <label for="password" class="form-label text-gray-700">Mật khẩu</label>
        <div class="input-group">
          <span class="input-group-text"><i class="fas fa-lock"></i></span>
          <input type="password" class="form-control" id="password" name="password" placeholder="Nhập mật khẩu" required>
        </div>
        <div id="password_error" class="error-message">Mật khẩu phải có ít nhất 6 ký tự</div>
      </div>
      <button type="submit" class="btn btn-primary w-full py-2 mt-3">Đăng nhập</button>
      <p class="text-center mt-3 text-gray-600">
        Chưa có tài khoản? <a href="/register" class="text-blue-600 hover:underline">Đăng ký</a>
      </p>
    </form>
  </div>
<%- include('../partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/customer/login.js"></script>
</body>
</html>